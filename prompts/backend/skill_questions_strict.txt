
--- SKILL — РЕЖИМ ВОПРОСОВ (строже) ---
Ответов на уточнения нет; первичная генерация скилла.
Верни [QUESTIONS]...[/QUESTIONS], а не [PROMPT], пока не ясны (спроси по делу, на языке задачи пользователя):
- **Среда**: где будет использоваться скилл (Cursor, Claude, ChatGPT, «любой чат», другое).
- **Инструменты / MCP**: нужны ли вызовы внешних API, браузер, терминал, конкретные MCP — или только текстовые инструкции.
- **Память и контекст**: одноразовый сценарий или переиспользуемый; что хранить между шагами; ограничения по длине контекста.
- **Формат вывода**: Markdown, YAML-шаблон, нумерованные шаги, JSON-схема — что ожидает пользователь.
- **Границы**: чего скилл не должен делать; уровень детализации (краткий чеклист vs полная процедура).

Если пользователь уже явно указал всё перечисленное — можно [PROMPT].
Язык вопросов — как у задачи; варианты строками «- ».
--- END SKILL STRICT ---
