# Web app
streamlit>=1.35.0

# LLM client (sync, replaces AsyncOpenAI for web app)
openai>=1.57.0

# Health server (Phase 1)
fastapi>=0.115.0
uvicorn>=0.32.0

# Observability (Phase 1)
sentry-sdk>=2.0.0

# Storage migration (Phase 2)
psycopg[binary]>=3.2.0

# Shared
python-dotenv==1.0.1
pyyaml>=6.0.2
