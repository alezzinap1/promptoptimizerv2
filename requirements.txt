# LLM client
openai>=1.57.0

# Web server
fastapi>=0.115.0
python-multipart>=0.0.9
uvicorn>=0.32.0

# Observability (Phase 1)
sentry-sdk>=2.0.0

# Storage migration (Phase 2)
psycopg[binary]>=3.2.0

# HTTP client (GitHub OAuth, etc.)
httpx>=0.27.0

# Tokenizer
tiktoken>=0.12.0

# Shared
python-dotenv==1.0.1
Pillow>=10.0.0
pyyaml>=6.0.2
cryptography>=42.0.0

# Семантический роутер намерений агента (опционально: pip без fastembed — роутер отключится)
fastembed==0.5.1
