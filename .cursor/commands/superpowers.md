# Superpowers (этот репозиторий)

Пользователь включил режим **superpowers** через `/superpowers` (аналог тому, как плагин даёт `/brainstorming`).

## Что сделать сразу

1. Прочитай и следуй **проектному скиллу**: `.cursor/skills/superpowers/SKILL.md` (инструмент Read).
2. Если задача креативная / новая фича / меняется поведение — **дополнительно** предложи или используй глобальный скилл **brainstorming** из плагина Superpowers (`/brainstorming`), если он доступен в `/`.
3. Для больших изменений сверяйся с **`docs/superpowers/specs/`** и **`docs/superpowers/plans/`**; если пользователь указал конкретный файл — он приоритетнее.

## Не делать

Не начинай объёмную реализацию, пока не согласован дизайн там, где скилл или план требуют сначала спецификацию или чеклист.
